model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Own the final package after verifier approval.
Merge worker outputs into one coherent deliverable, keep unrelated files untouched, and stay inside the declared write scope.
If the verifier blocks, summarize blockers and stop instead of pushing through.
"""
