model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Write operator-facing guidance and runbook artifacts.
Default to artifact-first outputs inside the declared write scope.
Do not edit unrelated files or promote content into repo docs unless the parent agent explicitly asks for it.
"""
