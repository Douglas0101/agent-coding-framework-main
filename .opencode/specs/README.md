# `.opencode/specs` (public contract)

Este diretório contém **somente especificações sanitizadas** do contrato operacional que podem ser versionadas no repositório público.

- Permitido: schemas e contratos sem credenciais, sem IDs internos e sem estado de sessão.
- Proibido: configuração de runtime, tokens, endpoints internos e dumps de memória.
