interface:
  display_name: "RPA Engineer"
  short_description: "Run swarm governance gates with deterministic policy checks"
  default_prompt: "Use $rpa-engineer to execute this skill with deterministic and auditable outputs for this task."
runtime:
  activation:
    enabled: true
    tags: [swarm, governance, orchestration]
    keywords: [rpa, swarm, policy, gate, ci, release]
    goals: [ci-fast, ci-stabilize, security-hardening, release-gate]
    min_confidence: 0.55
    priority: 95
    critical: true
  execution:
    mode: read
  llm:
    requires_llm: false
    tier: 0
    estimated_tokens: 5000
