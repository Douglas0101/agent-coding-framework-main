interface:
  display_name: "Auto Fixer"
  short_description: "Help with Auto Fixer tasks"
  default_prompt: "Use $auto-fixer to execute this skill with deterministic and auditable outputs for this task."
runtime:
  activation:
    enabled: true
    tags: [remediation, formatting, lint]
    keywords: [autofix, auto-fix, ruff, format, remediation, lint]
    goals: [ci-fast, ci-stabilize, release-gate]
    min_confidence: 0.6
    priority: 88
    critical: true
  execution:
    mode: write
  llm:
    requires_llm: false
    tier: 0
    estimated_tokens: 5000
