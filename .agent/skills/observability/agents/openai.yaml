interface:
  display_name: "Observability"
  short_description: "Help with Observability tasks"
  default_prompt: "Use $observability to execute this skill with deterministic and auditable outputs for this task."
runtime:
  activation:
    enabled: true
    tags: [observability, telemetry, tracing]
    keywords: [observability, telemetry, tracing, metrics, prometheus, otel]
    goals: [ci-fast, ci-stabilize, release-gate, security-hardening]
    min_confidence: 0.45
    priority: 72
    critical: false
  execution:
    mode: advisory
  llm:
    requires_llm: false
    tier: 0
    estimated_tokens: 15000
