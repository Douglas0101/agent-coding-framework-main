interface:
  display_name: "Backend Reliability"
  short_description: "Help with Backend Reliability tasks"
  default_prompt: "Use $backend-reliability to execute this skill with deterministic and auditable outputs for this task."
runtime:
  activation:
    enabled: true
    tags: [backend, reliability, api]
    keywords: [fastapi, openapi, auth, reliability]
    goals: [ci-stabilize, release-gate]
    min_confidence: 0.6
    priority: 78
    critical: false
  execution:
    mode: read
  llm:
    requires_llm: false
    tier: 0
    estimated_tokens: 5000
