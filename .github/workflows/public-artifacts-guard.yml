name: Public Artifacts Guard

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  guard-public-boundary:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate public/internal boundary
        run: ./scripts/check-public-boundary.sh
